version: '3.3'

services:
  robotics:
    build:
      context: ./
      dockerfile: robotics.Dockerfile
    ports:
      - "8888:8888"
      - "5000:5000"
    command: tail -F anything # will run container forever
